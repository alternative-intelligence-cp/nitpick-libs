# 0.2.0 — the rules and the board — DONE (2026-09-03)

> Paper only. Nothing in this subcycle touches a skill or a script. What it
> produces is the set of contracts every later subcycle is written against:
> the `W-` rules, the board schema, the record file, and the one mechanical
> edit each repository needs so the toolchain pin can be honoured.

## 1. Where you are

The workbench at `nitpick-libs/`. You are its single writer for this
subcycle (P-2). No stream is running; every library repository is unclaimed,
so the per-repository edits in step 5 are yours to make directly. Read
[`README.md`](README.md) §"Decisions taken at planning" first — every step
below implements one of them.

## 2. Decisions this subcycle lands

P-1, P-2, P-3, P-4, P-5, P-6, P-7, P-8, P-11, P-12, P-14, P-18 become
`W-15`…`W-26`; P-19 amends W-16. The wording below is the wording to use; each rule points back
at its `P-` for the reasoning rather than repeating it.

## 3. The steps

### Step 1 — the rules, in `WORKSTREAMS.md` §5

Append after W-14, in this order. Keep each to the length shown; the reasons
live in this cycle's README and are cited, not restated.

```
**Rule W-15 — the unit of delegation is the subcycle.** One fresh worker per
subcycle; the claim covers the repository for the cycle; the handoff between
subcycles is the execution record. (P-1)

**Rule W-16 — the workbench repositories have one writer: the orchestrator.**
A worker, planner, auditor or researcher writes nothing under `nitpick-libs/`
or `nitpick-apps/` roots. Findings for the playbook travel in the report and
the orchestrator lands them. The writer is named on the board's header
(`Workbench writer:`); a session that finds another named does not write
here. (P-2, P-19)

**Rule W-17 — workers commit to `main`.** Nothing is branched and nothing is
merged; one writer per repository makes both redundant. (P-3)

**Rule W-18 — the toolchain is pinned.** The orchestrator copies `npkc` and
`npkrt.o` out of the compiler's `build/` into `.internal/toolchain/<commit>/`,
records the commit on the board, and passes the paths to every worker. A
worker never reads `../nitpick/build/` and never builds the compiler. A re-pin
happens between cycles, by the orchestrator, and is recorded. (P-4)

**Rule W-19 — a claim is recoverable.** The board's in-flight table names the
subcycle, the agent label, the start time and the model for every claim. A
claim with no live agent in the current session is stale, and the orchestrate
skill's recovery procedure runs before any dispatch. (P-5)

**Rule W-20 — a report has one shape, in two places.** The worker's final
message and the subcycle's committed execution record both carry the REPORT
block the worker skill defines; `check_record.py` verifies the committed one.
(P-6)

**Rule W-21 — release needs a verifier.** Before a claim is released or a
subcycle is marked done on the board, a verifier re-runs the reference check,
the record check and the harness or probe commands on the committed tree.
Independently green is not green; reported green is not green either. (P-7)

**Rule W-22 — the audit precedes the close.** A cycle's last subcycle reports
READY-TO-CLOSE; the auditor runs and writes nothing; the orchestrator files the
report under `meta/audits/`; the close worker triages every finding. After
every third cycle close, an ecosystem-wide audit. (P-8)

**Rule W-23 — a stop stops its stream.** A compiler defect, a missing
decision, a wrong specification, a contested repository, a negative probe, or
an ambiguity stops the stream that found it and nothing else. Questions are
batched with a recommendation each and sent when every running stream is
stopped, the batch holds three, or four hours have passed. (P-11)

**Rule W-24 — width is an argument.** Default one; never more than one worker
per stream; helper agents do not count and never run two of a kind. (P-12)

**Rule W-25 — research has a shape.** One fetch may be inline; more goes to
the researcher agent, who never writes into a repository. The requesting
writer files the digest under `meta/research/` with the date checked; a
decision cites the digest; a security-sensitive digest is stale after ninety
days; language facts are never researched on the web. (P-14)

**Rule W-26 — repository creation is never delegated.** It is outward-facing
and it edits the registry, so the orchestrator or the author runs
`/npk:new-repo`. (P-18)
```

### Step 1b — the compiler-request registry

`check_refs.py` reported four undefined questions the moment the workbench
had a `meta/OPEN_QUESTIONS.md`: `O-N1`, `O-N2`, `O-N5`, `O-N6`, referenced by
`WORKSTREAMS.md`, `BOARD.md` and `LIBRARIES.md` and defined nowhere in this
repository — because `O-N` numbering is per repository and the numbers
collide (`O-N2` is the `npkg` gap in three repositories and three different
things in the other three). The registry in
[`../../OPEN_QUESTIONS.md`](../../OPEN_QUESTIONS.md) §"For the compiler" now
defines the four with every repository's local id. In this step: make
`WORKSTREAMS.md` §1's table and `BOARD.md`'s compiler-dependencies table
link to the registry in one sentence each, and add the rule to
`PLAYBOOK.md` §7's open-question paragraph: *"An `O-N` id used outside its
repository is the workbench registry's id."*

### Step 2 — the board schema

Rewrite `BOARD.md` to this skeleton. Keep every row that exists; only the
columns and the vocabulary change. The stream tables' `State` column carries
**only** the vocabulary in the legend; everything else about a claim lives in
the in-flight table, so that each fact has one place.

```
# The board

**Live state.** … (keep the opening paragraph and the ownership note)

**Last updated:** <date> · **Width:** <N> · **Toolchain:** <commit> ·
`.internal/toolchain/<commit>/` · pinned <date>
**Workbench writer:** none | <session id> since <time> — one writer here (W-16, P-19);
if this names a session that is not you, do not write in this repository

## Legend
| State | Means |
|---|---|
| `—` | not started, not claimed, nothing blocking it |
| `CLAIMED sN` | stream N owns this repository; the in-flight table says what it is doing |
| `BLOCKED on <repo> <cycle>` | cannot start until that cycle is DONE |
| `DONE` | closed, archived to `done/`, next cycle's opening file written |

## In flight
| Stream | Repository | Subcycle | Agent label | Since | Model | Note |
|---|---|---|---|---|---|---|
(one row per CLAIMED repository; the note is `tree dirty`, `audit filed`, `awaiting verify`, or blank)

## Questions for the author
| # | Stream | Raised | Question | Recommendation |
|---|---|---|---|---|
(moved to RECORD.md with the answer when answered)

## Claim protocol
1. The orchestrator writes `CLAIMED sN` in the stream table and a row in the
   in-flight table, and commits: `board: claim <repo> <subcycle> for sN`.
2. One worker works that subcycle. When it reports, the verifier runs.
3. On PASS the in-flight row advances to the next subcycle. At a cycle's
   close the claim advances to the repository's next cycle if its gate is
   ready, else to its next ungated cycle, else it is released and the row
   removed: `board: release <repo>`.
4. A claim with no live agent is stale — see the orchestrate skill §Recovery.

## Stream 1 — text / Stream 2 — data / Stream 3 — system
(the existing tables, State column on the new vocabulary; `READY` becomes `—`)

## The cross-stream gates            (unchanged)
## Compiler dependencies             (unchanged)
## Ready to start now                (unchanged)
```

The agent label format is `s<N>-<pkg>-<cycle>.<sub>-<HHMM>`, for example
`s2-ntime-0.0.0-1432`. It is what the orchestrator passes as the dispatch
description and what the recovery procedure matches against `ListAgents`.

### Step 3 — `LIBRARIES.md`

Replace the sentence *"Implementation waits on the compiler, and is
deliberately serial — one library at a time, beside the compiler's own
work."* with: *"Implementation is partitioned into streams by
`WORKSTREAMS.md`; the number of streams running at once is a per-session
choice, and one is always a coherent plan (W-5)."* — with `WORKSTREAMS.md`
as a relative link there, not here.

### Step 4 — `RECORD.md`

Create at the workbench root, the orchestrator's composed execution record
(the compiler's R8). Append-only, dated entries, never rewritten:

```
# The record

The orchestrator's execution record: what was dispatched, what came back,
what it cost, which estimates were wrong and by how much, which gates bound,
which findings recurred, and every question answered. Append-only. The board
is the present; this is the past.

## 2026-09-03
- (entries: `dispatch <label>`, `report <label> <status> <tokens> <minutes>`,
  `verify <label> PASS|FAIL`, `release <repo> <cycle>`, `pin <commit>`,
  `stale claim <repo>: <what was found, what was done>`, `question Q-n
  answered: <answer>`, `rebalance: <what moved and why>`)
```

Also create `meta/audits/README.md`: *"Audit reports, filed by the
orchestrator from the auditor's final message, named
`<repo>-<cycle>-<date>.md`. The auditor writes nothing; the close worker reads
these."*

### Step 5 — the toolchain edits, one commit per repository

In each of `nitpick-tui`, `nitpick-parse`, `nitpick-regex`, `nitpick-sockets`,
`nitpick-time`, and `../nitpick-apps/nitpick-posix`:

- `CLAUDE.md` §"Building and testing": replace the sentence beginning *"The
  compiler is at `../../nitpick`. Build it per its own `CLAUDE.md`."* with:
  *"The compiler binary is the **pinned toolchain** the board names
  (`../BOARD.md`, or `../../nitpick-libs/BOARD.md` from an application):
  `$NPKC` and `$NPKRT` are supplied to every session by the orchestrator, or
  set by hand from `.internal/toolchain/<commit>/`. Never build the compiler
  from here and never read its `build/` directly — the guard refuses the
  first and the second is rebuilt under you."* Keep the LLVM sentence.
- `meta/roadmap/0.0/0.0.0.md` §2: replace the literal
  `../../nitpick/build/npkrt.o` (or the application's equivalent depth) with
  `$NPKRT`, and add one line: *"`$NPKC` and `$NPKRT` come from the pinned
  toolchain; the board names the commit."*
- Commit subject: `workbench: use the pinned toolchain`, body citing W-18.
  Attribution per the harness notice.

Six repositories, twelve edits, six commits. Run `check_refs.py` on each
before committing.

### Step 6 — verify

```bash
python3 skills/check/scripts/check_refs.py . nitpick-tui nitpick-parse \
  nitpick-regex nitpick-sockets nitpick-time ../nitpick-apps ../nitpick-apps/nitpick-posix
```

All clean. Then commit the workbench: `0.2.0: the rules and the board`.

## 4. Acceptance

- [x] `WORKSTREAMS.md` §5 ends with W-26
- [x] the compiler-request registry is linked from `WORKSTREAMS.md` §1 and `BOARD.md`, and `PLAYBOOK.md` §7 carries the one-sentence rule
- [x] `BOARD.md` has the header lines including `Workbench writer:`, the in-flight table, the questions table, the four-word legend, and no `READY`
- [x] `LIBRARIES.md` no longer says serial
- [x] `RECORD.md` and `meta/audits/README.md` exist
- [x] six repositories' `CLAUDE.md` and `0.0.0.md` name `$NPKC`/`$NPKRT`, each in its own commit
- [x] `check_refs.py` clean across all eight roots
- [x] this file's title line says `DONE (<date>)` and §"Execution record" has the commit hashes

## 5. Watch for

- **The board is tracked and public.** The pin path on the board is
  relative. The absolute path goes in the worker prompt only.
- **`nitpick-posix` is two levels down** from the libraries' workbench; its
  `CLAUDE.md` already counts the levels correctly — keep that when editing.
- **Do not renumber anything.** W-1 stays discharged-but-kept as written;
  new rules append.

## Execution record

**2026-09-03 — executed by the author's planning session** (named on the board
as the workbench writer, P-19), in the order written.

- Steps 1, 1b, 2, 3, 4: `WORKSTREAMS.md` §5 ends with W-26 and §1 links the
  registry; `PLAYBOOK.md` §7 carries the registry sentence; `BOARD.md` is on
  the new schema with the writer line; `LIBRARIES.md` no longer says serial;
  `RECORD.md` and `meta/audits/README.md` exist. All in this commit (HEAD).
- Step 5, one commit per repository, subject `workbench: use the pinned
  toolchain`: `nitpick-tui` e5439ee, `nitpick-parse` 3cad08c,
  `nitpick-regex` c056ae1, `nitpick-sockets` d385991, `nitpick-time` aad6e45,
  `nitpick-posix` 948d9b6. Every tree clean afterwards; `check_refs` clean on
  each before committing. `nitpick-posix` is the one repository outside
  `nitpick-libs` this subcycle touched, because the plan named it.
- Step 6: `check_refs.py` clean across all eight roots.

Findings:

- **Two edits per repository were three.** Each `0.0.0.md`'s "Where you are"
  bullet also pointed at the compiler's `build/` — *"`build/npkc` from `npkg
  build`, or `.internal/quickemit/npkc` … whichever is current"* — which
  contradicts the pin as directly as the §2 command did. Edited with it.
  `nitpick-posix/CLAUDE.md` had no "Building and testing" section at all; it
  gained a short "The toolchain" section instead.
- **An anchored edit that writes nothing on a miss is worth the retries it
  cost.** The workbench script stopped three times on anchors guessed from
  memory rather than read from the file, and left the tree untouched each
  time. A `sed` would have half-applied. Read the target, then edit.
- **The record's own commit is HEAD**, as 0.2.1's L-1 will require of every
  worker; this subcycle followed that rule before it was written into the
  skill, and it held.

