# 0.2.2 — the orchestrate skill, and `START.md` — DONE (2026-09-03)

> The orchestrator is the role that runs the loop, and today the loop is
> split between a skill that describes the role and a pasted prompt that
> describes the procedure, with the stop list only in the prompt. This
> subcycle gives the loop, the stop list, dispatch, pinning, recovery and
> escalation one home, and reduces `START.md` to an invocation.

## 1. Where you are

`skills/orchestrate/SKILL.md` (3 KB) and `START.md`. 0.2.0 and 0.2.1 are DONE:
the rules exist, the board has its in-flight table, and the worker's REPORT
block is defined. The agent names used below (`npk:worker`, `npk:verifier`,
`npk:auditor`, `npk:researcher`, `npk:planner`) are confirmed by 0.2.4; until
then the skill says *"the worker agent (0.2.4); before it exists, a
general-purpose agent told to load `/npk:worker` first"*.

## 2. Decisions

P-4, P-5, P-7, P-8, P-10, P-11, P-12, P-13, P-17, P-18. Two local:

- **L-1 — a claim persists across cycles while the stream keeps the
  repository.** At a cycle close the orchestrator *advances* the claim to the
  next cycle if the gate is ready, or *releases* it when the stream leaves the
  repository. *Declined:* release-and-reclaim at every close (two commits per
  close that say nothing).
- **L-2 — `tick` is a mode of the same skill.** `/npk:orchestrate tick` does
  one pass of the loop and ends its turn; it is what a keepalive re-runs.
  *Declined:* a separate skill (a second home for the loop).

## 3. The new `SKILL.md`

Frontmatter: `description` as now plus "reads its arguments";
`argument-hint: "[width=N] [start=<repo> <cycle>.<sub>] [streams=1,2,3] [tick]"`;
`allowed-tools` extended with `Agent`, `Write`, `Bash(cp *)`, `Bash(mkdir *)`,
`Bash(sha256sum *)`, `Bash(cmp *)`, `Bash(find *)`, `Bash(llvm-config *)`,
`Bash(ls *)`, `Bash(date *)`. The body uses `$ARGUMENTS`.

**§0 Arguments.** `$ARGUMENTS` is a space-separated list of `key=value`
tokens or the word `tick`.

| Argument | Default | Meaning |
|---|---|---|
| `width=` | `1` | maximum concurrent workers, one per stream (W-24) |
| `streams=` | per W-5 for the width: `1`, `1,2`, `1,2,3` | which streams run |
| `start=` | `board` | `board`: each free stream takes its next item per W-9; `<repo> <cycle>.<sub>`: that item, on the stream that owns the repository, before anything else |
| `tick` | — | one pass of §5 and stop; §13 |

An argument that does not parse is a stop: say what was given and what is
accepted.

**§1 The role, and its one hard rule.** Keep the first paragraph. Replace the
quoted paragraph about `disallowed-tools` with: *"This skill cannot enforce
that. A skill's `allowed-tools` only pre-approves; the tool sets that restrict
are the agent definitions under `agents/`, and the orchestrator is the main
session. The rule is a discipline and this document is its text."*

**§2 Startup** (not in `tick` mode):

1. `mkdir -p .internal && echo "${CLAUDE_SESSION_ID}" > .internal/orchestrator.session`
   — the marker 0.2.6's compaction hook keys on.
2. Read, in order: `BOARD.md`; `WORKSTREAMS.md` §3 and §5; `LIBRARIES.md`;
   `../nitpick-apps/APPS.md`; `git -C ../nitpick log --oneline -3`.
3. The toolchain: if the board's header names no pin, or the pinned
   directory is missing, or `sha256sum -c` against its `SHA256SUMS` fails,
   run §3. Otherwise use it. Never re-pin while any claim is in flight.
4. Recovery, §4, for every `CLAIMED` row.
5. Tell the author the picture in under ten lines: width, pin, each stream's
   next item and its gate, anything recovered, anything on the questions
   table. Then §5.

**§3 The pin procedure.**

```bash
COMMIT=$(git -C ../nitpick log -1 --format=%h)
# a build younger than two minutes may be mid-rebuild: do something else and retry
[ -z "$(find ../nitpick/build/npkc -mmin -2)" ] || echo "NOT YET"
PIN=.internal/toolchain/$COMMIT && mkdir -p "$PIN"
cp ../nitpick/build/npkc ../nitpick/build/npkrt.o "$PIN"/
cmp ../nitpick/build/npkc "$PIN/npkc" && cmp ../nitpick/build/npkrt.o "$PIN/npkrt.o"
( cd "$PIN" && sha256sum npkc npkrt.o > SHA256SUMS )
llvm-config --version                      # must print 20.1.2
printf 'compiler %s\nllvm %s\npinned %s\n' "$COMMIT" "$(llvm-config --version)" "$(date -Is)" > "$PIN/PIN.md"
```

Then the board header: `**Toolchain:** <commit> · .internal/toolchain/<commit>/ · pinned <date>`,
commit `board: pin toolchain <commit>`, and a `pin <commit>` line in
`RECORD.md`. Copying *out* of the compiler tree is a read; the guard allows
it. The absolute paths go to workers in the prompt, never onto the board.

**§4 Recovery.** For each `CLAIMED` row, find its in-flight row and run
`ListAgents`. A label with a live agent is fine. A label with none is stale:

| Subcycle file's title | `git -C <repo> status --porcelain` | Do |
|---|---|---|
| `RUNNING` | dirty | re-dispatch the same subcycle with `TREE: dirty` and a `NOTES:` line saying the predecessor died |
| `RUNNING` | clean | re-dispatch the same subcycle; the work was lost |
| `DONE` | clean | run the verifier; on PASS advance or release; on FAIL re-dispatch with the FAIL in `NOTES:` |
| `DONE` | dirty | a record written and not committed: as `RUNNING` + dirty |
| `PLANNED` | any | the worker never started: re-dispatch |

Every recovery is a `stale claim` line in `RECORD.md`. After a session
restart *every* claim is stale, because `ListAgents` shows only this
session's agents.

**§5 The loop.** For each stream in `streams=` that has no live worker and is
not stopped, and while live workers are fewer than `width=`:

1. **Pick.** The stream's current repository is the one it has `CLAIMED`, or
   else the next in its `WORKSTREAMS.md` §3 order with state `—`. The
   subcycle is the first not `DONE` in the repository's current cycle
   (`meta/roadmap/ROADMAP.md`, then the cycle `README.md`'s subcycle table).
   If the cycle's opening subcycle file is missing, dispatch the planner
   for it first and treat that as the stream's item.
2. **Gate.** A cross-stream dependency must be `DONE` on the board, not
   `CLAIMED`. Not ready → the next ungated cycle in the same repository
   (W-9). None → the stream idles; say so in `RECORD.md` and on the picture.
3. **Claim.** Stream table `CLAIMED sN`; in-flight row with the label
   `s<N>-<pkg>-<cycle>.<sub>-<HHMM>`, the time, and the model; commit
   `board: claim <repo> <cycle>.<sub> for sN`.
4. **Dispatch.** The worker agent with §6's prompt, `description` = the
   label, `model` per P-17. It runs in the background; you are woken when
   it reports.
5. **On a report**, §7. **Verify** before anything moves on the board.
6. **Advance or release.** A subcycle PASS advances the in-flight row. A
   cycle close (a `DONE` after the audit) advances the claim to the next
   cycle if its gate is ready, else to the next ungated cycle, else releases
   the claim (`board: release <repo>`) and checks whether any `BLOCKED` row
   just became free (W-9).
7. **Record.** One line per event in `RECORD.md`. Commit the workbench with
   the board change.
8. Repeat. If nothing is running and nothing can be dispatched, send the
   batch (§9) and end the turn.

**§6 The dispatch template.** Send exactly these lines, then nothing else —
the skill the agent preloads carries the procedure:

```
STREAM: sN
REPO: <absolute path>
SUBCYCLE: <cycle>.<sub>
TOOLCHAIN: NPKC=<absolute> NPKRT=<absolute> COMMIT=<commit>
ATTRIBUTION: <your harness notice's trailer lines, verbatim>
TREE: clean | dirty
AUDIT: none | <absolute path under meta/audits/>
DIGESTS: none | <absolute paths>
NOTES: none | <free text: a verifier FAIL, a predecessor's death, an answer from the author>
```

Before the worker agent exists (0.2.4), prefix one line: *"Load `/npk:worker`
first; it is your procedure."*

**§7 On a report.** Run `check_record.py <repo> <id>` first; a finding is a
re-dispatch with the finding in `NOTES:`. Then by status:

| Status | Do |
|---|---|
| `DONE` | verifier (§8). PASS → §5.6. FAIL → re-dispatch, FAIL text in `NOTES:` |
| `READY-TO-CLOSE` | verifier. PASS → auditor with `REPO` and the cycle; file its final message as `meta/audits/<repo>-<cycle>-<date>.md`; commit; re-dispatch the worker with `AUDIT:` naming it |
| `BLOCKED` | if `for-the-author` names a dispatch error (a missing input, a claim mismatch, a tree state) fix it and re-dispatch; otherwise the stream stops and the item goes to the questions table |
| `STOPPED` | a compiler defect. The stream stops (W-11). Questions table: the reproduction path, recommendation "raise against the compiler". Never worked around |
| `NEEDS-DECISION` | the stream stops; the question and the worker's recommendation go to the questions table |
| `RED` | the stream stops (W-12: never a retry); the harness line goes to the questions table |

`findings-for-playbook` lines go into `RECORD.md` under the report line; the
orchestrator decides whether each becomes a playbook edit and makes that edit
itself (W-16). `open-questions-raised` are noted; they live in the repository.

**§8 Verification.** Dispatch the verifier with `REPO`, `SUBCYCLE`,
`TOOLCHAIN`, and the report's `harness:` line. It answers
`VERIFY <repo> <id> PASS|FAIL` with one line per check. Its procedure is in
its agent definition (0.2.4) and in the check skill.

**§9 Escalation — the stop list and the batch.** These stop a stream:

1. a compiler defect (`STOPPED`)
2. a decision that is not in a `DECISIONS.md` (`NEEDS-DECISION`)
3. a specification wrong in a way that changes a plan
4. two streams needing one repository
5. a probe returning negative and a repository's shape in question
6. anything ambiguous enough that you would be guessing

Each goes onto the board's questions table with a recommendation — not a
menu. Send the batch when **every running stream is stopped**, when the
table holds **three**, or **four hours** after the first unanswered item:
a push notification if the tool is available, then the question — with
`AskUserQuestion` when it fits four options, otherwise a message. While
waiting, other streams keep running; when nothing runs, end the turn. An
answer becomes a `question answered` line in `RECORD.md`, the row leaves the
table, and the stream restarts with the answer in `NOTES:`.

**§10 Rebalancing.** Keep as written (W-4), and add: the measured pace goes
in `RECORD.md` as `rebalance:` lines, and a move between streams is the
orchestrator's decision, recorded there.

**§11 The record.** Keep the paragraph and name the file: `RECORD.md`,
append-only, the entry vocabulary 0.2.0 gave it.

**§12 Merging.** Replace the section with: *"There is no merge step. Every
repository has one writer (W-7) and workers commit to `main` (W-17). The
compiler's cumulative-prefix protocol (`../nitpick/meta/roadmap/ORCHESTRATION.md`
§4) applies only where several writers share one tree, which nothing here
does. Independently green is still not green: that is why the verifier runs
on the committed tree (W-21), and why the ecosystem-wide audit runs after
every third close (W-22)."*

**§13 `tick`.** Skip §2. Read `BOARD.md`. Handle any report notifications
already delivered (§7). Run §5 once. If the batch conditions hold, send it.
End the turn. This is what `/loop /npk:orchestrate tick` re-runs.

**§14 Files.** `WORKSTREAMS.md` durable; `BOARD.md` live, yours; `RECORD.md`
past, yours; `meta/audits/` yours; `.internal/toolchain/` yours and untracked.

## 4. `START.md`

Replace the whole file with:

```
# Starting the orchestrator

In `nitpick-libs`, with the plugin loaded (`claude --plugin-dir ~/Workspace/REPOS/nitpick-libs`
until the flagless load in meta/roadmap/0.2/0.2.6.md is confirmed):

    /npk:orchestrate width=1

Arguments — `width=1|2|3` (default 1) · `streams=1,2` (default per W-5) ·
`start=<repo> <cycle>.<sub>` · `tick`. Optional keepalive, once the loop is
running: `/loop /npk:orchestrate tick`.

The loop, the stop list, the dispatch template, the pin, the recovery and the
escalation rule are all in `skills/orchestrate/SKILL.md`. There is nothing
else to paste.

## What it will and will not do while you are away
(keep the current two paragraphs)

## The guard
(keep, minus the case count — it prints its own)
```

## 5. Acceptance

- [x] the skill has §0–§14 as above (and a §15 file list); `grep -n 'disallowed-tools' skills/orchestrate/SKILL.md` is empty
- [x] the stop list appears in the skill and nowhere else (`grep -rn 'STOP AND ASK' .` is empty)
- [x] `START.md` is under forty lines (35) and contains no procedure
- ~~a dry read: a session started with `/npk:orchestrate width=1` on the current board reports the picture and stops at "dispatch" because no worker agent exists yet~~ — struck: this session has no plugin loaded, so the dry read moves to 0.2.4 step 2, the live session that confirms the agent names; the expected result there is the same
- [x] `check_refs.py .` clean; committed `0.2.2: the orchestrate skill and START.md`

## 6. Watch for

- **`$ARGUMENTS` is one string.** `start=nitpick-time 0.0.0` is two tokens;
  the skill must say the repository and the subcycle are the two tokens
  after `start=`.
- **The orchestrator's context grows by one report per subcycle.** That is
  by design; 0.2.7 measures it. Do not paste harness output into the
  conversation — the verifier summarises.
- **A notification can arrive while the orchestrator is mid-step.** Finish
  the board commit before handling it; the board must never be half-edited
  when a report is processed.

## Execution record

**2026-09-03 — executed by the author's planning session.**

- `skills/orchestrate/SKILL.md` rewritten: §0 arguments, §1 the role with
  the corrected statement about what a skill can enforce, §2 startup, §3 the
  pin, §4 recovery, §5 the loop, §6 the dispatch template, §7 on a report,
  §8 verification, §9 the stop list and the batch, §10 rebalancing, §11 the
  record, §12 models, §13 `tick`, §14 no merge step, §15 files. 12 KB.
- `START.md` cut to an invocation and the two paragraphs kept; 35 lines.
- The agent names are written as `npk:*` with the general-purpose fallback
  line until 0.2.4 confirms them.

Deviations: the dry read is struck into 0.2.4 (above). `README.md` still
describes `START.md` as "the paste-able orchestrator startup prompt"; that
sentence is 0.2.6's, with the rest of the README.


**2026-09-03, from 0.2.4's test 6.** The agent names are confirmed as
written and §6's parenthetical says so; the general-purpose fallback line
now applies to a session whose startup lists no `npk:` agents rather than
to "until the worker agent exists". §0 gained a "Given: `$ARGUMENTS`" line,
because the placeholder also sat in the prose and the loaded text read
"`width=1` is a space-separated list…" — harmless, but a weaker executor
should not have to see through it.

**2026-09-03, from 0.2.6's loud case.** `ListAgents` lists this machine's
peer sessions — a short name derived from the working directory, an
idle/busy state, an age — which §2.1 said it could not. The takeover step
now reads that listing first: a `nitpick-libs-…` peer that is not you means
stop and ask; none, with the work committed and the record hours old, means
take the lock. The listing shows no UUID, so the mapping to the board's
writer line is by directory, not by id. §4's "shows only this session's
agents" stays: the in-flight table's agents are subagents.

**2026-09-03, from 0.2.7's precondition check.** The pin procedure (§3)
copied the binary without recording whether the compiler tree was clean.
The `950bb1d` pin was taken at 17:12, two and a half hours after that
commit, from a tree that by 17:49 carried eighteen modified files, a
running harness for the compiler's next subcycle, and a rebuilt `npkc` that
no longer matched the pinned one; no build anywhere in the compiler tree
matches the pinned bytes, so whether they are the commit's own build cannot
be told after the fact. §3 now computes `tree clean|dirty` from
`git status --porcelain`, writes it into `PIN.md`, and carries it onto the
`pin` line in `RECORD.md`. It does not refuse a dirty pin: the compiler
session works for hours at a time, and a fixed binary with a recorded hash
is still what W-18 wants — the word is what the record needs.
