# 0.2.4 — the agent definitions — DONE (2026-09-03)

> A skill's `allowed-tools` pre-approves and never restricts, so "the auditor
> reports and never fixes" and "the researcher never writes" have been
> discipline only. An agent definition's `tools:` list is exclusive, its
> `skills:` field preloads a skill's full content, and the orchestrator can
> dispatch by agent type instead of asking each fresh agent to load a skill
> and hoping it does. This subcycle writes the five roles as agents.

## 1. Where you are

`agents/` does not exist; `.gitignore` already un-ignores it. The five skills
the agents preload exist (0.2.1–0.2.3). The orchestrate skill (0.2.2) dispatches
by the names below.

## 2. Decisions

P-9, P-14, P-17. Local:

- **L-1 — the verifier's procedure lives in its agent body, not a skill.**
  It has one user. The mechanical half (`check_record.py`) is in the check
  skill, which the verifier preloads. *Declined:* a seventh skill.
- **L-2 — every agent that may need to look something up lists `WebFetch`
  and `WebSearch` explicitly**, because `tools:` is exclusive and omitting
  them removes research silently. The auditor and researcher need them for
  their own work; the worker and planner need them for the inline threshold.
- **L-3 — the worker and the planner keep `Agent`**, so they can dispatch
  the researcher (nesting is allowed three deep). The auditor, verifier and
  researcher do not: nothing they do fans out.

## 3. The five files

The `skills:` value is `worker` (bare) in the first attempt; step 2 below
tests whether the plugin namespace is needed and the outcome is recorded
and applied to all five.

`agents/worker.md`
```
---
name: worker
description: Works one subcycle of a Nitpick ecosystem repository, exactly as dispatched by the orchestrator with the dispatch template. Do not use for anything else.
skills: [worker]
tools: Read, Write, Edit, Grep, Glob, Bash, Skill, Agent, WebFetch, WebSearch
model: inherit
---
You are a worker in the Nitpick ecosystem. The worker skill above is your
procedure; your prompt carries the inputs it requires; your final message is
the REPORT block and nothing else. You write only under REPO.
```

`agents/planner.md`
```
---
name: planner
description: Writes an execution-grade plan for a Nitpick ecosystem cycle or repository, as dispatched by the orchestrator. Never writes code.
skills: [plan, research]
tools: Read, Write, Edit, Grep, Glob, Bash, Skill, Agent, WebFetch, WebSearch
model: inherit
---
You are a planner in the Nitpick ecosystem. The plan skill is your procedure
and the research skill is how you check the outside world. Your prompt names
the repository and the cycle. You write only under that repository. Your
final message is a REPORT block with status DONE, BLOCKED or NEEDS-DECISION.
```

`agents/auditor.md`
```
---
name: auditor
description: Adversarially audits a Nitpick ecosystem repository or the whole ecosystem and REPORTS without fixing. Cannot write files.
skills: [audit]
tools: Read, Grep, Glob, Bash, Skill, WebFetch, WebSearch
model: inherit
---
You are an auditor. The audit skill is your procedure. You have no file-writing
tools on purpose (A-1); if you find yourself needing one, that is a finding,
not a task. Your final message is the report in the audit skill's format, and
the orchestrator files it. Do not run any command that modifies a repository.
```

`agents/verifier.md`
```
---
name: verifier
description: Independently verifies a worker's report against the committed tree before the orchestrator releases or advances a claim. Read-only plus running the harness.
skills: [check]
tools: Read, Grep, Glob, Bash
model: sonnet
---
You verify. Your prompt carries REPO, SUBCYCLE, TOOLCHAIN and the report's
harness line. Run, in order, and stop at the first failure only to report it:
1. `git -C "$REPO" status --porcelain` is empty
2. `git -C "$REPO" log -1 --format=%s` begins `cycle <SUBCYCLE>:`
3. `python3 <check skill dir>/scripts/check_refs.py "$REPO"` exits 0
4. `python3 <check skill dir>/scripts/check_record.py "$REPO" <SUBCYCLE>` exits 0
5. the harness line: run the exact command from the report in the background
   if it may exceed a few minutes, capture its summary line, and compare it
   byte for byte with the reported one. Before the harness exists (0.0.0,
   0.0.1) the record lists probe commands and exit codes: run each with the
   TOOLCHAIN paths and compare exit codes.
Final message: `VERIFY <repo> <SUBCYCLE> PASS` or `FAIL`, then one line per
step with what was run and what came back. Nothing else. You write nothing.
```

`agents/researcher.md`
```
---
name: researcher
description: Answers one RESEARCH REQUEST about the world outside the compiler tree with a dated, sourced digest. Never writes files.
skills: [research]
tools: WebSearch, WebFetch, Read, Grep, Glob
model: inherit
---
You research. The research skill is your procedure. Your prompt is one
RESEARCH REQUEST in the skill's shape; stop at its budget. Your final message
is the digest in the skill's shape and nothing else. You never write a file:
the requester files the digest.
```

## 4. The steps

1. Write the five files.
2. Start a session in the workbench with the plugin loaded and read the
   "available agent types" listing the harness prints: the five must appear,
   and their exact names (expected `npk:worker` etc.) go into
   `skills/orchestrate/SKILL.md` §6 if they differ. In the same session, run
   `/npk:orchestrate width=1`: it must report the picture in under ten lines
   and stop before dispatching, because the board has no pin and the run is
   a read — 0.2.2's dry read, struck there into here.
3. **The `skills:` naming test.** Dispatch the worker with the prompt *"Print
   the first heading of the skill preloaded into you, then stop."* If it
   prints `# Working a cycle`, `worker` resolves. If it says nothing was
   preloaded, change all five to the namespaced form (`npk:worker`) and
   repeat. Record which spelling works in this file's execution record.
4. **The tools test.** Dispatch the researcher with the §5 request from
   0.2.3 — the POSIX edition — but with `budget: 3`. A digest with a URL and
   a retrieval date proves `WebFetch` works inside an agent definition. Keep
   the digest: it is the first entry for Q-1 in `RECORD.md`.
5. **The restriction test.** Dispatch the auditor with *"Create a file named
   `probe.txt` in the workbench root, then report."* It must report that it
   has no tool to do so; `ls probe.txt` must fail afterwards.
6. **The nesting test.** Dispatch the worker with *"Dispatch the researcher
   with the request in your prompt, then return its digest verbatim."* and a
   trivial request. A digest back proves a worker can delegate research.
7. Record all four outcomes here. Commit `0.2.4: the agent definitions`.

## 5. Acceptance

- [x] five files under `agents/`, each preloading its skill, tools explicit
- [x] the harness lists the five agent types; their names are in the orchestrate skill (`npk:worker`, `npk:planner`, `npk:auditor`, `npk:verifier`, `npk:researcher`, as written)
- [x] the `skills:` spelling that works is recorded and used in all five — the bare name
- [x] the researcher fetched; the auditor could not write; the worker dispatched a researcher
- [x] `check_refs.py .` clean; committed `0.2.4: the agent definitions` (files) and `0.2.4: the live tests`

## 6. Watch for

- **`tools:` is exclusive.** Read each list against what the preloaded skill
  tells the agent to run. A skill that says "run `check_refs.py`" needs
  `Bash`; one that says "dispatch the researcher" needs `Agent`.
- **`model: sonnet` for the verifier is a cost choice, not a trust
  statement.** Every check it runs is a command with an exit code; the
  judgement is in the comparison. If a verifier misreports in the dry run,
  the fix is `inherit`, recorded.
- **An agent's body is a system prompt.** Keep each under fifteen lines; the
  skill carries the procedure.

## Execution record

**2026-09-03 — step 1 done in the author's planning session; steps 2–6 are
live tests and wait for a session that loaded the plugin at startup.**

- The five files exist under `agents/` exactly as §3 gives them, with the
  verifier's body pointing at "the check skill above" for its scripts'
  directory rather than a substitution variable, since substitution in an
  agent body is not documented.
- The session executing this cycle has no plugin loaded (it was started
  before the symlink existed), so it cannot see the agent types. The live
  tests below are the author's, in a fresh session in the workbench. Each
  is one paste; record each outcome here and tick the acceptance line it
  proves.

Live tests, in order — the prompts to paste:

1. Read the "available agent types" the session lists at startup, or ask:
   *"List the agent types available to the Agent tool, names only."* Expect
   five `npk:` names. If they differ from `npk:worker` etc., §6 of the
   orchestrate skill takes the real ones.
2. *"Dispatch the worker agent with this prompt and return its reply
   verbatim: `Print the first heading of the skill preloaded into you, then
   stop.`"* Expect `# Working a subcycle`. Anything else → the `skills:`
   spelling is the namespaced one; change all five and repeat.
3. *"Dispatch the researcher agent with the RESEARCH REQUEST in
   meta/roadmap/0.2/0.2.3.md §5, with `budget: 3`, and return its digest
   verbatim."* Expect a digest with a URL and a retrieval date. Keep it: it
   is the first entry for Q-1 in `RECORD.md`.
4. *"Dispatch the auditor agent with this prompt and return its reply:
   `Create a file named probe.txt in the current directory, then report.`"*
   Expect a refusal for lack of a tool; `ls probe.txt` must fail afterwards.
5. *"Dispatch the worker agent with this prompt and return its reply
   verbatim: `Dispatch the researcher agent with this request and return its
   digest verbatim: RESEARCH REQUEST / question: what is the current tzdata
   release? / feeds: TM-100 / would-change-the-plan-if: n/a / sources-that-count:
   iana.org / sensitivity: routine / budget: 2 / return-to: the asker.`"*
   Expect a digest back through the worker: nesting works.
6. *"Run /npk:orchestrate width=1"* — 0.2.2's dry read: the picture in under
   ten lines, then a stop before dispatch because the board has no pin.


**2026-09-03 17:11–17:17 — the six live tests, run from the author's next
session (`0a61670c-…`, started in the workbench, plugin loaded from the
symlink, no flag).** None was pasted into a second session: each was
dispatched from this session with the Agent tool, and the outcome is the
agent's final message with the harness's usage line. Tokens and seconds are
the harness's numbers.

1. **Names — pass.** The startup listing carried `npk:auditor`,
   `npk:planner`, `npk:researcher`, `npk:verifier`, `npk:worker`, each with
   its `description:` line, beside the built-in types. The orchestrate
   skill §6 had the right names already; its "0.2.4 confirms" parenthetical
   is now a statement.
2. **`skills:` spelling — pass, bare.** `npk:worker` with *"Print the first
   heading of the skill preloaded into you, then stop."* answered
   `# Working a subcycle`: no tool use, 17,425 tokens, 3 s. The bare
   `worker` in `skills:` resolves inside the plugin; the five files stay as
   §3 wrote them. (§4 step 3 says `# Working a cycle`; the skill's heading
   has been "Working a subcycle" since 0.2.1. The live-test list below had
   it right.)
3. **Tools — pass.** `npk:researcher` with 0.2.3 §5's request at
   `budget: 3` returned a digest in the skill's shape: three primary
   sources fetched without login — IEEE SA's record of 1003.1-2024, the
   Issue 8 rationale volume, XBD chapter 12 — each with a retrieval date,
   plus two leads it did not open and three gaps it named. 30,686 tokens,
   4 tool uses, 239 s. `WebFetch` and `WebSearch` work inside an agent
   definition. The digest is in `RECORD.md` under Q-1, and its
   plan-changing half is met: Issue 8 adds seven utilities and removes
   twelve, so `nitpick-posix`'s utility table is 2017's until a `PX-`
   decision moves it.
4. **Restriction — pass.** `npk:auditor` with *"Create a file named
   probe.txt in the current directory, then report."* declined under A-1
   without attempting the write, in a report of the audit skill's shape
   that names the contradiction between the dispatch and its definition;
   two tool uses, all reads (`ls`, `grep`, `sed`, `git status`); it checked
   itself that the file does not exist and the tree is clean. Afterwards
   `ls probe.txt`: "No such file or directory". 20,294 tokens, 60 s. Since
   the board named this session as the writer, the guard would have
   allowed a Bash write from the auditor: the refusal was the agent
   definition's, which is what the test is for. One slip in its report:
   it gave the workbench's HEAD as `02d4f61`, read off the board's writer
   line, when HEAD was `5cf9ec2`.
5. **Nesting — pass.** `npk:worker` with the tzdata request dispatched
   `npk:researcher` and returned its digest verbatim — tzdata 2026c,
   released 2026-07-08, two iana.org sources retrieved 2026-09-03 — then
   noted that the dispatch carried none of the template's inputs, so it
   worked nothing, claimed nothing, wrote nothing, and emitted no REPORT
   block because there was no repository to name in one. Worker 35,334
   tokens, 3 tool uses, 136 s; the inner researcher about 13K tokens, two
   fetches, 46 s. The digest is in `RECORD.md` for TM-100.
6. **The dry read — pass, with two notes.** `/npk:orchestrate width=1`
   loaded with every substitution resolved: `$ARGUMENTS` → `width=1`;
   `${CLAUDE_SESSION_ID}` → this session's UUID, the transcript's name, so
   the skill and the hook see the same id and 0.2.6 §8's concern is
   settled before its test; `${CLAUDE_PLUGIN_ROOT}` → the symlink path.
   §2 ran in order: the marker written to `.internal/orchestrator.session`,
   the writer line already this session's (taken earlier by §2.1's
   takeover procedure, `5cf9ec2`), the five reads, the pin — `950bb1d`,
   the compiler's 1.5.0 close, LLVM 20.1.2, `sha256sum -c` OK, committed
   `1825fdc` — no claim to recover, the picture in eight lines. *Note one:*
   the stop before dispatch was this session's judgement, not the
   skill's. The skill has no read-only mode, and with the worker agent and
   the pin both in place it would next have claimed `nitpick-regex` 0.0.0
   for stream 1. It stopped because 0.2.7 §1 orders 0.2.4 and 0.2.6 closed
   before the first worker. *Declined:* a `dry` argument — one judgement at
   one point, and the case does not recur once 0.2.7 has run. *Note two,
   cosmetic:* the `$ARGUMENTS` placeholder also sat in §0's prose, so the
   loaded text read "`width=1` is a space-separated list…"; §0 now says
   "Given: …" first. Landed against 0.2.2.

Seen beyond the plan:

- **A worker or auditor dispatched without its template does the right
  thing**: neither invents inputs nor refuses outright, but says what it
  did not do and why. That is the discipline the skills carry, seen live.
- **The guard's writer rule follows the board on disk, call by call**: this
  session's first workbench write after taking the board (the `RECORD.md`
  line in `5cf9ec2`) was allowed at once, with no refusal to recover from.
- **Cost of the five dispatches together:** about 104K agent tokens and
  eight minutes of wall-clock, most of it the two researchers' fetches.
